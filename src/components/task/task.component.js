import React, { useState } from 'react';

const Task = ({task}) => {
  
    return (
      <div>
        <h4>{task}</h4>
        <button onClick={}>x</button>
      </div>
    )
  }

export default TaskSet;